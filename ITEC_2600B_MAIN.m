%START OF THE MAIN

%prompt if sitter or looking for sitter
    %prompts user to search or enter information

    %based on selection two options will be prompted 


        %if search
        %user inputs all specifics that they want (ex. age, breed, animal, etc)
        %algoritm to seach through the database

        %if enter
        %user inputs all specifics and then store in database

%based on either user is a sitter or not use the required algorithm
%finds pair 
     